extends: existence

message: "Repetitive word found: '%s'. Consider changing '%s' to '%s'."
level: warning

# Regex to match repetitive words (e.g., "the the", "and and")
ignorecase: true
nonword: true
tokens:
  - '\b(\w+)\s+\1\b'

action:
  name: suggest
  params:
    - RepetitionFix.tengo
