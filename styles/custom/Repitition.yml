extends: existence

message: "Repetitive word found: '%s'. Consider revising."
level: warning
# Regex to match repetitive words (e.g., "the the", "and and")
ignorecase: true
nonword: true
tokens:
  - '\b(\w+)\s+\1\b'

action:
  name: edit
  params:
    - regex
    - '\b(\w+)\s+\1\b'
    - '\1'
