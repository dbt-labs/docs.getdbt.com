# Agent skills

Canonical location for all agent skills in this repo. `.claude/skills` is a symlink
to this directory — don't add skills there directly, add them here.

- **Claude Code:** invoke with `/skill-name`
- **Codex:** invoke with `$skill-name`

Each skill lives in its own folder with a `SKILL.md` (frontmatter + instructions).
