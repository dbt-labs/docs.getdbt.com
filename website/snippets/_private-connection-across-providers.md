Private connection endpoints can't connect across cloud providers (AWS, Azure, and GCP). For a private connection to work, both <Constant name="dbt" /> and the server (like {props.type}) must be hosted on the same cloud provider. For example, <Constant name="dbt" /> hosted on AWS cannot connect to services hosted on Azure, and <Constant name="dbt" /> hosted on Azure can’t connect to services hosted on GCP.
