A snapshot captures the state of your data at the time it runs. If a record changes multiple times between snapshot runs, the snapshot may only record the state observable at the next run and may not capture intermediate changes. Choose a schedule that matches the change rate you need to analyze.

