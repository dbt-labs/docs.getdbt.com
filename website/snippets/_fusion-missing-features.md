| <div style={{width:'240px'}}>Feature</div> | <div style={{width:'200px'}}>This will affect you if...</div> | <div style={{width:'240px'}}>GitHub issue</div> |
|---------|----------|---------|
| [Programmatic invocations](/reference/programmatic-invocations) | You use dbt Core’s Python API for triggering invocations and registering callbacks on events/logs. Note that <Constant name="fusion" />’s logging system is a work in progress. | [dbt-fusion#10](https://github.com/dbt-labs/dbt-fusion/issues/10) |
| [Linting using SQLFluff](/docs/deploy/continuous-integration#to-configure-sqlfluff-linting) | You use SQLFluff for linting in CI or local development. SQLFluff is not natively compatible with the <Constant name="fusion_engine"/> but we provide a [SQLFluff compatible high performance alternative](/reference/commands/lint?version=2.0) with <Constant name="fusion" />. Support varies by where you run it: <br /><br />**<Constant name="dbt_platform"/> CI jobs**: SQLFluff linting is not available when running on <Constant name="fusion"/>. <br />**Studio IDE**: SQLFluff linting works, but uses the <Constant name="core"/> engine templater rather than <Constant name="fusion"/>. <br />**Local development**: You can run SQLFluff locally using the standalone <Constant name="core"/> engine templater as a workaround. A native <Constant name="fusion"/> linter is available with [`dbt lint` command](/reference/commands/lint?version=2.0) | [dbt-fusion#11](https://github.com/dbt-labs/dbt-fusion/issues/11) |

