Replace `YOUR_DBT_HOST_URL` with your hostname (for example, `abc123.us1.dbt.com`). Supported clients self-register on first connect (dynamic registration); others need an admin to register them manually in <Constant name="dbt_platform" />. For the full OAuth flow, scopes, and limitations, refer to [OAuth (remote MCP)](/docs/dbt-ai/setup-remote-mcp#oauth-remote-mcp) and [Connect apps with OAuth](/docs/platform/manage-access/connect-apps-oauth).
