:::info `profiles.yml` file is for dbt Core and users only
If you're using <Constant name="dbt_platform" />, you don't need to create a `profiles.yml` file. This file is only when you use dbt Core or run dbt Fusion locally. To connect your data platform to <Constant name="cloud" />, refer to [About data platforms](/docs/cloud/connect-data-platform/about-connections). 
:::
