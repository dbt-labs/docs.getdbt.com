To help you tune `lag_tolerance` values, the **dbt State** page on the <Constant name="dbt_platform" /> provides [lag tolerance recommendations](/docs/deploy/dbt-state-interface#lag-tolerance-recommendations) based on observed upstream data refresh patterns, so you can see which models would benefit from a higher tolerance.
