:::info Requires sign-in
This capability requires a [free dbt platform account](/docs/sign-in-dbt-extension). It is not available during the 14-day trial period. Existing registered users have access automatically.

<VersionBlock firstVersion="2.0">

Authentication is handled by [`dbt login`](/reference/commands/login), so your login state is shared across dbt features like dbt VS Code extension and dbt State.

</VersionBlock>
:::
