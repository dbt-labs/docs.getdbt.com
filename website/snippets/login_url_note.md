:::success Logging in

Users can sign in at [https://login.dbt.com](https://login.dbt.com) to view accounts they have access to across instances and choose where to open <Constant name="dbt_platform" />. This is the recommended entry point for most users.

For SSO through your identity provider, you can also use the following URL format with your account login URL slug. Replace `LOGIN_SLUG` with the value from the previous steps and `YOUR_ACCESS_URL` with the [appropriate Access URL](/docs/platform/about-platform/access-regions-ip-addresses) for your region and plan:

`https://YOUR_ACCESS_URL/enterprise-login/LOGIN-SLUG`

Account administrators can turn account discovery on or off with **Enable global account discovery** in [Account settings](/docs/platform/account-settings#enable-global-account-discovery).

:::
