<span>{props.title} includes both resource properties and configs. Some fields on this page are properties (for example, <code>description</code> and resource-specific properties) and are defined in YAML files (for example, <code>schema.yml</code>). Configs nested under <code>config</code> can also be set in <code>dbt_project.yml</code> or using <code>config()</code>. Refer to <a href="/reference/configs-and-properties">Configs and properties</a>, <a href="/reference/define-configs">Define configs</a>, and <a href="/reference/define-properties#which-properties-are-not-also-configs">Define properties</a> for more information.</span>
