---
title: "Models object schema"
sidebar_label: "Model"
id: "discovery-schema-environment-applied-model"
---
 
import { NodeArgsTable, SchemaTable } from "./schema";
<!-- NEED  UPDATE THIS DESCRIPTION -->
The models object allows you to query information about models. 

The [Example query](#example-query) illustrates a few fields you can query with the `models` object. Refer to [Fields](#fields) to view the entire schema, which provides all possible fields you can query.

### Arguments

When querying for `models`, you can use the following arguments:

<NodeArgsTable parent="AppliedState" name="models" />

### Example query

<!-- NEED  UPDATE THIS EXAMPLE -->
You can use the `environmentId` and the model's `uniqueId` to return the model and its execution time for the last 20 times it was run, regardless of which job ran it.

```graphql
query {
  environment(id: 834) {
    applied {
      models (first: 100) {
        edges {
          node {
            name
            description
            access
            accountId
            catalog {
              owner
            }
            config
            environmentId
            tests {
              name
              description
            }
          }
        }
      }
    }
  }
}
```

### Fields

When querying for `models`, you can use the following fields:

<SchemaTable nodeName="ModelAppliedStateNodeConnection" />
