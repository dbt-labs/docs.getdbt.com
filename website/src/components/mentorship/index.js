import React from 'react';

// this is component, file name doesn't matter. the way it's exported determines how component looks. index is main root file in any directory. 

export default function Mentorship() { //mentorship component
    return (
        <div>
            <h1>Mentorship testing</h1>
        </div>
    )
}
